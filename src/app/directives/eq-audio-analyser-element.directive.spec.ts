import { EqAudioAnalyserElementDirective } from './eq-audio-analyser-element.directive';

describe('EqAudioAnalyserElementDirective', () => {
  it('should create an instance', () => {
    const directive = new EqAudioAnalyserElementDirective();
    expect(directive).toBeTruthy();
  });
});
